
// Move the ADUTab.tsx to the tabs directory
// This file is a placeholder as we don't have the original content
import React from "react";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export const ADUTab = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Average Daily Usage (ADU)</CardTitle>
        <CardDescription>
          Track and analyze your product's average daily usage
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>ADU visualization and management content will be displayed here.</p>
      </CardContent>
    </Card>
  );
};
